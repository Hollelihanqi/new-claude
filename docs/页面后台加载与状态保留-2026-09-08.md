# 页面后台加载与状态保留（2026-09-08）

- 页面保持独立懒加载文件，启动后分批在后台预热，首页不静态引入全部菜单代码。
- 菜单切换只改变可见性，保留表单、勾选、筛选和滚动状态。
- 再次进入时静默更新环境配置、WorkBuddy、MCP、扩展、用量、诊断及设置环境信息；手动刷新仍可用。
- 后台更新不覆盖表单草稿和 WorkBuddy 未保存勾选。WorkBuddy 更新期间重复进入合并请求；失败保留旧模型列表。
- 非当前页的弹窗和抽屉不显示；图表使用容器尺寸观察，在可见且具有宽度后初始化。
- 如果启动后立即点击尚未完成预热的页面，仍可能短暂显示准备提示；首次数据请求耗时取决于本地环境和网关。

验证：本机真实 Tauri WebView 中切换七个菜单，均只有当前页可见，无空白或整页准备提示；调用记录确认后台数据接口实际执行。洞察图表宽高分别为 1472×360、1472×300。WorkBuddy 切页后 DOM 节点与未保存勾选保留，测试结束恢复勾选，未保存配置。

构建初始 JavaScript（入口及 HTML 预加载项）约 471 KB，此前约 470 KB；用量图表约 587 KB，继续作为独立异步文件。后台预热会陆续加载这些文件，但不增加首页必须同步加载的依赖范围。
