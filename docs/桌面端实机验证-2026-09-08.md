# 桌面端实机验证

本轮已在 Windows 正常桌面权限下启动 Tauri 开发版，并连接该桌面窗口内的 WebView2 操作实际页面。没有使用独立浏览器页面代替桌面应用，也没有模拟后端成功响应。

## 窗口确认

- 可执行文件：`E:\Dr\new-claude\src-tauri\target\debug\cc-switch.exe`。
- 实际窗口标题：Claude 管理中心；Windows 检查结果为可见、未最小化。
- 页面存在 Tauri IPC，能够读取真实环境配置、执行网关模型检测。
- 之前仅依据沙箱内进程或系统输入法覆盖层句柄判断应用已打开不成立。此次通过正常桌面权限启动后，真实应用窗口和 WebView2 页面均已确认。

## 检查结果

| 检查项 | 实际结果 |
| --- | --- |
| jdw 检测模型 | 返回 3 个模型，显示成功提示，页面未白屏 |
| 重复档位配置 | Opus 和 Haiku 均保留 claude-opus-5；打开 Opus 下拉正常，无重复选项渲染异常 |
| 重复点击检测 | 再次检测成功，表单仍正常 |
| 已保存凭证读取 | 健康检查使用本机已有 Key，hq、ba、ds、jdw 网关均可达，分别返回 16、48、16、3 个模型 |
| WorkBuddy | 现有 xai 组织的模型和证书状态成功加载；证书显示已信任 |
| 刷新后进入新增组织表单 | 新增表单保持无模型选项，未看到旧请求模型串入；未保存该表单 |
| 洞察 | 折线图和饼图均创建 Canvas，实际截图显示趋势图；无组件异常 |
| 诊断 | 真正检查完成后显示健康，并分别展示四个网关检测结果与同步日志 |
| MCP、扩展、设置、帮助 | 页面均可切换，无页面错误边界提示 |
| 高级设置 | jdw 的编辑器正常加载，内容可解析为 JSON；未保存修改 |
| 前端异常 | 所检查流程未捕获到未处理的 error 或 unhandledrejection |

## 实机发现并修复的问题

首次进入洞察页，Vite 才发现拆分后的 ECharts 模块，日志显示依赖优化完成后触发整页重载，导致界面回到环境页。

已在 `vite.config.js` 的 `optimizeDeps.include` 中列出 `echarts/core`、`echarts/charts`、`echarts/components`、`echarts/renderers`，让开发启动时提前预构建。配置生效后重新验证：进入洞察页正常生成两个图表，同一页面的标记仍存在，未再次触发整页重载。这项配置不改变生产包的按需加载方式。

## 验证边界

- 当前只有一个真实 WorkBuddy 组织，因此没有创建第二个组织验证跨组织保存；请求乱序由既有自动化测试覆盖。
- 实机没有保存、删除环境或组织，也没有修改证书、Key 或 settings.json；应用启动自身的既有同步流程仍正常运行。
- 加密保存、文件冲突与故障返回等分支由已有测试覆盖；本轮未故意破坏真实文件或让真实网关失效。
- 本轮重新执行生产构建、类型检查、前端 45 项测试，均通过。
- macOS 实机和远程双平台 CI 尚未在本轮验证。
- 开发版保持运行，最后停留在 jdw 检测成功页面。

## 截图（**已移除，不再随仓库提供**）

> 2026-09-12 说明：本页原先链接 7 张桌面端截图。它们**从未纳入版本控制**，
> 现已按"清理临时测试产物"的要求删除，**不可恢复**。
>
> 删除理由有两条（第二条是更硬的那条）：
> 1. 它们是 2026-09-08 那轮验证的产物，界面此后已大幅变化，作为"当前状态"的证据已失效；
> 2. **仓库的 `publishSafety.test.ts` 护栏只扫文本文件**，截图里的内网地址、环境名
>    不会被它拦下 —— 而本仓库要对外公开。
>
> 本页上方的**文字记录仍然是这一轮验证的正式证据**（它逐条写了做了什么、看到什么）。
> 需要图像证据时，请按 [`双平台真机验证清单-2026-09-12.md`](./双平台真机验证清单-2026-09-12.md)
> **重新采集并当场核对内容**后再提交。
