# 项目开发强制约束

## Windows 与 macOS 必须同步兼容

1. 每次新增功能、修复缺陷或调整行为时，必须同时评估并实现 Windows 与 macOS，禁止只修改当前开发电脑所使用的平台。
2. 涉及路径、安装目录、可执行文件、应用包、进程发现、注册表、钥匙串、证书、权限、Shell 命令、文件选择器、启动方式和系统 API 时，必须分别实现 Windows 与 macOS 的等价行为。
3. Windows 使用的 `.exe`、注册表、`certutil`、反斜杠路径等逻辑，不得直接复用于 macOS；macOS 使用的 `.app`、应用包目录、`/Applications`、Unix 路径和系统命令也不得直接复用于 Windows。
4. 前端不得硬编码单一平台的文件扩展名、路径、按钮文案或操作说明。平台差异必须由后端明确返回，界面根据平台展示正确行为。
5. 自动检测、手动选择、配置持久化、导入、启动、更新后修复等完整流程必须在两个平台保持同等能力，不能只保证代码能够编译。
6. 如果某项能力尚未在 Windows 与 macOS 同时完成，则该功能不得标记为完成、不得宣称跨平台兼容、不得创建发布 Tag。

## 测试与发布门禁

1. 每次修改都必须补充或更新能够覆盖 Windows 与 macOS 分支的回归测试。平台路径等纯逻辑应设计成可在任一开发平台同时测试两个分支，不能只依赖当前操作系统的 `cfg` 测试。
2. 提交前必须运行并通过：
   - `pnpm build`
   - `pnpm test -- --run`
   - `cargo test`
   - `cargo fmt -- --check`
   - `git diff --check`
3. 涉及平台相关代码时，必须由 GitHub Actions 的 `windows-latest` 和 `macos-latest` 同时验证。单个平台通过不代表完成。
4. Release 工作流中的 Windows 测试、macOS 测试、Windows 构建和 macOS 构建必须全部成功，才能向用户报告发布完成。
5. 不得把“CI 可以编译”表述为“功能已经在该平台验证”。没有真实设备端到端验证时，必须明确说明验证边界。
6. 任一测试失败时，必须读取具体失败步骤和日志、修复根因、增加回归测试，并在新的版本 Tag 中重新发布；禁止覆盖已经推送的公开 Tag。

## 分支纪律

1. 本仓库永远只使用 `main` 分支。禁止自行新建任何其他分支（包括 `feature/*`、`fix/*`、`docs/*`、`wip/*`、`chore/*` 等），禁止创建 worktree 来规避本约束。
2. 禁止自行执行 `git checkout -b`、`git switch -c`、`git branch <新分支名>`、`git worktree add` 等任何创建分支的操作。
3. 所有工作（功能、修复、文档、审计报告）一律直接在当前 `main` 分支上进行，完成后在 `main` 上提交并推送。
4. 同步远程更新时只允许对 `main` 做快进更新，不得为此新建分支或临时分支。
5. 禁止对 `main` 执行 `git push --force`、`git reset --hard` 后强推等会重写已推送历史的操作。
6. 如确有需要新分支的场景，必须先把理由和分支用途告知用户，取得用户明确指令后才可执行；不得自行判断后直接创建。

## 测试后的清理

1. 每轮测试（`pnpm dev`、Tauri 桌面应用、E2E、真机验证等）结束后，必须主动关闭本轮启动的所有进程，包括 dev server、应用进程、以及被父进程遗留的子进程；不得留下占用端口的残留进程。
2. 收尾时必须核对端口占用（如 1420 / 5199 / 9223 等），确认本轮启动的监听端口已释放。
3. 测试过程中临时新增的代码、脚本、mock 数据、临时配置、临时构建产物，测试完必须全部删除，不得留在工作区、不得提交。
4. 清理只针对本轮自己启动的进程与新增文件；不得擅自终止用户自己启动的、与本轮无关的进程。
5. 清理完成后如实报告：哪些进程被关闭、哪些临时文件被删除、还有什么残留未处理。

## 完成检查

每次交付前必须逐项确认：

- Windows 功能实现完成。
- macOS 功能实现完成。
- 两个平台的自动检测和手动兜底均已覆盖。
- 两个平台的用户提示和错误信息准确。
- 本地完整测试通过。
- Windows 与 macOS CI 通过。
- 如涉及发布，两个平台的安装包构建均通过。
- 全程只在 `main` 分支完成，未自行新建任何其他分支或 worktree。

只要其中任何一项未满足，就必须明确报告尚未完成，不得给出“已经完全兼容”或“已经全部修复”的结论。
